<% js_form_for :user, @user do |f| %>
	<%= f.password_field :password %>
	<%= f.password_field :password_confirmation %>
	<%= f.hidden_field :id, :value => params[:id]%>
	<%= f.submit tt("user.new_password.button_create_new_password") %>
<% end %>
