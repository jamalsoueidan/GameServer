<% js_form_for :user do |f| %>
	<%= f.text_field :email %>
	<%= f.submit_or_cancel :submit => {:label => tt("user.forgot_password.button_forgot_password")}, :cancel => {:label => tt("user.cancel"), :url => {:action => :login}} %>
<% end %>
